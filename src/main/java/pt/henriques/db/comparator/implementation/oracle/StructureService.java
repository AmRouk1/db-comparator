package pt.henriques.db.comparator.implementation.oracle;

import org.springframework.stereotype.Service;

import lombok.RequiredArgsConstructor;

@Service
@RequiredArgsConstructor
public class StructureService {

//	private final UserTableRepository tableRepository;
//	private final UserTablespaceRepository tablespaceRepository;
//	private final UserIndexRepository indexRepository;
//
//	public DefinitionStructure retreveStructure() {
//		return DefinitionStructure.builder()
//									.tableStrucures(tableRepository.findAll())
//									.tablespaces(tablespaceRepository.findAll())
//									.indexStructures(indexRepository.findAll())
//									.build();
//	}
}